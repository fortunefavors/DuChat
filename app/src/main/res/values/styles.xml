<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="FullScreenAppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="CustomDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@drawable/dialoa_bg</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="ConfirmButton">
        <item name="android:layout_width">@dimen/dialog_buttonWidth</item>
        <item name="android:layout_height">@dimen/dialog_buttonHeight</item>
        <item name="android:background">@drawable/solid_buton_bg</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/dialog_content_textSize</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="MyTablayoutstyle" >
<!--        <item name="tabBackground">@color/common_blue</item>-->
        <item name="tabIndicatorColor">@color/white_70</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabGravity">center</item>
        <item name="tabMode">fixed</item>
        <item name="tabSelectedTextColor">@color/white</item>
        <item name="android:textSize">50sp</item>
        <item name="android:textColor">@color/white_70</item>
    </style>

    <style name="DenyButton">
        <item name="android:layout_width">@dimen/dialog_buttonWidth</item>
        <item name="android:layout_height">@dimen/dialog_buttonHeight</item>
        <item name="android:background">@drawable/stroke_buton_bg</item>
        <item name="android:textColor">@color/common_blue</item>
        <item name="android:textSize">@dimen/dialog_content_textSize</item>
        <item name="android:gravity">center</item>
    </style>

</resources>